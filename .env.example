# Base de datos — dejar vacío para SQLite local, o usar PostgreSQL URL
DATABASE_URL=

# Master API key para crear nuevas API keys (cambiar en producción)
MASTER_API_KEY=dev-master-key-cambiar-en-prod

# Flask
FLASK_ENV=development
PORT=5000
